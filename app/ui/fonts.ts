import { Inter, Lusitana, Lilita_One, Roboto_Slab } from 'next/font/google';

export const lilitaOne = Lilita_One({
    subsets: ['latin'], // Specify any required subsets
    weight: '400',
});

export const robotoSlab = Roboto_Slab({
    subsets: ['latin'], // Specify any required subsets
    weight: '400',
});